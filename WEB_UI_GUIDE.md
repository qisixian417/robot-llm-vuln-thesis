# RoboGuard Web界面使用说明

## 🚀 快速启动

### 方法1：使用启动脚本（推荐）
```bash
cd /Users/yuan/Desktop/robot-llm-vuln-thesis
./start_ui.sh
```

### 方法2：直接运行
```bash
cd /Users/yuan/Desktop/robot-llm-vuln-thesis
streamlit run app.py
```

启动后，在浏览器中访问：**http://localhost:8501**

## 📋 功能介绍

### 1. 主界面布局

**左侧边栏**：
- ⚙️ 系统信息：显示RAG知识库大小、检测模型
- 📖 使用说明：快速上手指南
- 📊 项目信息：系统简介

**主界面**：
- 🔍 漏洞检测：输入代码进行检测
- 📝 示例代码：快速加载测试用例

### 2. 检测流程

1. **输入代码**
   - 在文本框中粘贴要检测的函数代码
   - 选择代码语言（C++或Python）

2. **开始检测**
   - 点击"🔍 开始检测"按钮
   - 系统会自动：
     - 从RAG知识库检索相关历史漏洞案例
     - 使用LLM分析代码
     - 返回结构化的检测结果

3. **查看结果**
   - **漏洞状态**：是否检测到漏洞（红色/绿色提示框）
   - **漏洞类型**：CWE编号和名称
   - **置信度**：0-100%（颜色编码：绿色=高，橙色=中，红色=低）
   - **原因说明**：详细解释为什么存在漏洞
   - **参考知识**：展开查看RAG检索到的历史案例

### 3. 示例代码

点击"📝 示例代码"标签页，可以快速加载预设的测试用例：

- **缓冲区溢出 (C++)** - 使用`strcpy`导致的缓冲区溢出
- **命令注入 (Python)** - 使用`os.system`导致的命令注入
- **内存泄漏 (C++)** - 提前返回导致的内存泄漏
- **安全代码 (Python)** - 正常的安全代码示例

## 🎨 界面特性

### 视觉设计
- **响应式布局**：自适应不同屏幕尺寸
- **颜色编码**：
  - 🔴 红色：检测到漏洞
  - 🟢 绿色：代码安全
  - 🟠 橙色：中等置信度
- **代码高亮**：自动语法高亮显示

### 交互功能
- **实时检测**：点击按钮即可检测
- **一键清空**：快速清除输入
- **示例加载**：一键加载示例代码
- **折叠面板**：RAG知识可展开/折叠

## 📊 输出说明

### 检测结果包含5个关键字段：

1. **has_vulnerability** (是否存在漏洞)
   - `true`: 检测到漏洞
   - `false`: 未检测到明显漏洞

2. **vulnerability_type** (漏洞类型)
   - 格式：`CWE-XXX: 漏洞名称`
   - 例如：`CWE-119: Buffer Overflow`

3. **reason** (原因说明)
   - 详细解释为什么存在这个漏洞
   - 包含代码分析和风险说明

4. **confidence** (置信度)
   - 范围：0.0 - 1.0
   - 显示为百分比（如：95%）

5. **retrieved_knowledge** (检索到的参考知识)
   - 从595个历史漏洞案例中检索的相关案例
   - 每个案例包含：
     - 代码片段
     - CWE编号
     - 严重性级别
     - 来源仓库

## 🎓 毕业答辩使用建议

### 1. 演示流程

**开场**（1分钟）：
- 打开Web界面，展示系统概览
- 介绍左侧边栏的系统信息（595个案例）

**功能演示**（3-4分钟）：
1. 点击"缓冲区溢出"示例
2. 点击"开始检测"
3. 展示检测结果：
   - 指出检测到CWE-119
   - 展示置信度95%
   - 展开RAG检索结果，说明系统从历史案例中学习

4. 再演示一个"命令注入"案例
5. 展示RAG检索到的`gazebosim/gz-sim`历史漏洞

**对比实验**（2分钟）：
- 切换到终端，展示对比实验结果
- 说明静态工具召回率0%，RAG+LLM召回率100%

### 2. 截图建议

论文中可以使用的截图：

1. **系统主界面**
   - 展示整体布局和设计
   - 标注各个功能区域

2. **检测结果示例**
   - 漏洞检测成功的案例
   - 展示5个输出字段

3. **RAG检索结果**
   - 展开的参考知识面板
   - 展示历史案例的元数据

4. **示例代码页面**
   - 展示4个预设测试用例

### 3. 回答评委问题

**Q: 为什么选择函数级检测？**
A: 静态工具更适合行级检测（单行危险函数），而函数级检测需要理解整个函数的逻辑流程，这正是RAG+LLM的优势。实验证明静态工具在函数级检测上召回率为0%，而我们的系统达到100%。

**Q: RAG的作用是什么？**
A: RAG从595个ROS历史漏洞案例中检索相关知识，帮助LLM识别相似的漏洞模式。例如在检测命令注入时，系统检索到了gazebosim项目的CWE-78历史漏洞，并明确引用了这个案例。

**Q: 系统的局限性？**
A: 主要有两点：1) 精确率50%，存在误报；2) 计算成本较高，需要调用LLM API。未来可以通过优化prompt和增加二次验证来降低误报率。

## 🔧 技术细节

### 系统架构
```
用户输入代码
    ↓
RAG检索器 (ChromaDB)
    ↓ (检索3条相关案例)
VulnDetectorAgent
    ↓ (调用千问LLM)
结构化输出
    ↓
Web界面展示
```

### 依赖组件
- **前端**: Streamlit (Python Web框架)
- **后端**: VulnDetectorAgent (漏洞检测)
- **RAG**: ChromaDB + text-embedding-v2
- **LLM**: 千问-plus (DashScope API)

### 性能优化
- 使用`@st.cache_resource`缓存检测器初始化
- 异步执行检测任务
- 响应式布局提升用户体验

## 📝 常见问题

### Q: 启动失败怎么办？
A: 检查以下几点：
1. `.env`文件是否配置正确
2. `DASHSCOPE_API_KEY`是否有效
3. 向量数据库是否已构建（`data/chroma_db/`）

### Q: 检测速度慢？
A: 正常情况下检测需要5-10秒，因为需要：
1. RAG检索（查询向量数据库）
2. LLM推理（调用API）
如果超过30秒，检查网络连接和API配额。

### Q: 如何添加新的示例代码？
A: 编辑`app.py`中的`EXAMPLES`字典，添加新的示例。

### Q: 可以部署到服务器吗？
A: 可以！修改`start_ui.sh`中的`--server.address`为`0.0.0.0`即可外网访问。

## 📞 支持

如有问题，请检查：
1. 项目README.md
2. ANALYSIS_STATIC_VS_RAG.md（对比实验分析）
3. experiments/目录下的实验结果

---

**RoboGuard** - 基于RAG增强的ROS函数级安全漏洞检测系统
