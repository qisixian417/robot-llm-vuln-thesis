# 项目完整流程说明（从数据集构建到检测与修复）

> 更新日期：2026-06-10
> 项目包含两个系统：漏洞检测（工作量1）+ 漏洞修复（工作量2）

---

## 第零阶段：数据集版本说明

| 版本 | 路径 | 用途 | 特点 |
|---|---|---|---|
| v1（旧）| `data/` | 检测基线实验 | 865条，随机负样本，有噪音 |
| **v2（新）** | **`data_v2/`** | **检测+修复实验** | **成对数据，277条测试集，0 None CWE** |

**当前使用 v2 数据集。**

### 1.4 数据独立性验证
`scripts/verify_data_independence.py`

- 三重检查：ID比对 + 代码哈希比对 + 仓库级比对
- 发现并删除 6 条重复，最终 RAG 语料 316 条，与测试集 0 重叠

### 1.5 KL-RAG 知识库构建
`scripts/build_knowledge_base.py`

- 对 316 条漏洞代码，LLM 提炼 4 个字段：functional_semantics / root_cause / trigger_condition / fix_pattern
- 输出：`data/rag_knowledge_v1.jsonl`（316条）

---

## 第二阶段：环境配置与向量数据库构建

### 2.1 配置文件
`.env`：MODEL_NAME=qwen-max / DASHSCOPE_API_KEY / EMBEDDING_MODEL=text-embedding-v3
`configs/agent.yaml`：RAG top_k / Chroma 路径等参数

### 2.2 构建向量数据库

**代码级 Chroma**（普通 RAG 用）：
- 命令：`python scripts/build_chroma_db.py --reset --corpus data/rag_corpus_v2_filtered.jsonl`
- 存到：`data/chroma_db/`（316条漏洞代码向量，1024维）

**知识级 Chroma**（KL-RAG 用）：
- 命令：`python scripts/build_kl_chroma_db.py --reset`
- 存到：`data/chroma_kl_db/`（316条知识描述向量，1024维）

---

## 第三阶段：RAG 模块（rag/ 目录）

| 文件 | 功能 | 效果 |
|---|---|---|
| `rag/retriever.py` | Naive Dense 检索（text-embedding-v3 + Chroma）| 基础，支持 CWE/language 过滤 |
| `rag/bm25_retriever.py` | BM25 关键词精确匹配检索 | 补充语义检索的盲区 |
| `rag/hybrid_retriever.py` | BM25 + Dense 双路召回，RRF 算法融合 | +1.3% F1 |
| `rag/hyde.py` | LLM 将代码改写为假设性漏洞描述再检索 | **有害 -2% F1（负向发现）** |
| `rag/reranker.py` | FlashRank Cross-Encoder 精排（ONNX，无需GPU）| +3.7% F1 |
| `rag/corrective.py` | CRAG：LLM 评估检索质量，差则重试 | +3.2% F1 |
| `rag/knowledge_retriever.py` | **KL-RAG**：代码→LLM提炼知识→知识语义匹配 | **最大贡献，Recall 0.53→0.75** |

---

## 第四阶段：Agent 模块（agents/ 目录）

| 文件 | 功能 | 效果 |
|---|---|---|
| `agents/router.py` | C1: 预判 CWE 类型，用于定向检索 | 配合 KL-RAG 有效，单独用 F1 下降 |
| `agents/cwe_prompts.py` | C2: 9种 CWE 专用 Prompt（含 Few-Shot + CoT）| 聚焦注意力 |
| `agents/vuln_detector.py` | C2: Detection Agent，根据 CWE hint 选专用 prompt | 核心检测模块 |
| `agents/subagents.py` | C5+C6: Voting（3人设投票）/ Debate（辩论） | Voting 降误报；Debate 有害（负向发现）|
| `agents/reflection.py` | C7: Reflection Agent，检测后自我反思 | **有害 -9% F1（负向发现）** |
| `agents/plan_and_solve.py` | C8: 三步分析法（识别→追踪→检查）| Recall=0.944 但 FP=122 |
| `agents/sandbox.py` | C3: Docker 沙箱（ASan/TSan/UBSan）| PoC 运行环境 |
| `agents/verifier.py` | C3: Verification Agent，生成 PoC + 沙箱验证 | 最佳 Precision，降误报 |
| `agents/ast_tool.py` | C9: tree-sitter 解析代码结构，提取危险函数 | 提供结构化信息给 LLM |

---

## 第五阶段：Pipeline 调度（pipeline/ 目录）

`pipeline/coordinator.py`：LangGraph 状态机，串联所有模块

**节点流程**：
```
router → retrieve → detect → [verify?] → finalize
```

**支持开关**：enable_kl_rag / enable_hybrid / enable_reranker / enable_crag /
enable_router / enable_voting / enable_debate / enable_reflection /
enable_plan_and_solve / enable_ast_tool / enable_verifier

---

## 第六阶段：一段代码进来的完整处理路径

```
输入：一段 C++ 或 Python 函数代码

Step 1 [router.py]
  LLM 预判 CWE 类型
  输出：{primary:"CWE-119", confidence:0.8, use_filter:true}

Step 2 [knowledge_retriever.py]  ← 当前最优配置
  代码 → LLM 提炼知识描述
    → text-embedding-v3 向量化
    → 在 chroma_kl_db 中检索 top-5 相似知识案例

  或 [hybrid_retriever.py + corrective.py]  ← 代码级备选
  代码 → BM25 + Dense 双路召回 → FlashRank 精排 → CRAG 质量评估 → top-5

Step 3 [vuln_detector.py + cwe_prompts.py]
  代码（加行号）+ RAG案例 + CoT指令
    → 按 CWE 类型选专用 prompt（含 Few-Shot 示例）
    → LLM 推理（4步CoT: 识别→追踪→检查→结论）
  输出：{has_vulnerability:true, vulnerability_type:"CWE-119",
         confidence:0.85, vulnerable_lines:[...], cot_reasoning:"..."}

Step 4（可选）[verifier.py + sandbox.py]
  LLM 生成 PoC（含 ROS 类型桩 + main()）
    → Docker 容器编译运行（g++ -fsanitize=address）
    → 解析 ASan 输出
  输出：{verified:"confirmed", evidence:"stack-buffer-overflow", confidence:0.95}

Step 5 [coordinator.py _finalize_node]
  综合 Detection + Verification → 最终 label 和置信度
  输出：{
    label: "vulnerable_verified",
    has_vulnerability: true,
    vulnerability_type: "CWE-119: Buffer Overflow",
    confidence: 0.95,
    reason: "...",
    verification_status: "confirmed"
  }
```

---

## 第七阶段：实验评估

### 单配置评估
`scripts/evaluate.py`
```bash
python scripts/evaluate.py --config kl_rag_only
# 对 test_v2_filtered.jsonl 161条逐一运行，算 P/R/F1
# 结果 → experiments/eval_kl_rag_only_xxx.json
```

### 消融实验（10组，代码级RAG）
`scripts/run_ablation.py`
```bash
python scripts/run_ablation.py --skip-verifier
# 1_naive → 2_rag_basic → ... → 10_full_with_verifier
# 结果 → experiments/ablation/
```

### 全量消融（8组，KL-RAG 系列）
`scripts/run_overnight_ablation.py`
```bash
python scripts/run_overnight_ablation.py
# 在完整161条上跑，约7-9小时
# 结果 → experiments/overnight/
```

### 传统工具对比
`scripts/run_cppcheck_baseline.py`
```bash
python scripts/run_cppcheck_baseline.py
# Cppcheck 2.20 对C++样本，F1=0.359
# 结果 → experiments/eval_cppcheck_baseline.json
```

### 出图
`scripts/plot_results.py`
```bash
python scripts/plot_results.py
# 三张图 → experiments/figures/
```

---

## 第八阶段：实证研究（论文第6章）

`scripts/empirical_study_v3.py`（主入口）
`scripts/empirical_analysis/`（10个RQ子模块）

| RQ | 文件 | 内容 |
|---|---|---|
| D-RQ1 | rq01_taxonomy.py | CWE分布/Shannon熵/卡方 |
| D-RQ2 | rq02_component.py | 组件漏洞密度/Fisher检验 |
| D-RQ3 | rq03_complexity.py | 代码复杂度/Mann-Whitney U |
| D-RQ4 | rq04_language.py | C++/Python对比/Odds Ratio |
| D-RQ5 | rq05_api_features.py | ROS API关联/PMI/BH-FDR |
| C-RQ1 | rq06_code_patterns.py | 漏洞反模式 |
| D-RQ6 | rq08_repository.py | 仓库集中度/Gini=0.797 |
| C-RQ2 | rq10_predictive.py | 随机森林预测/AUC=0.94 |

结果 → `experiments/v3/`

---

## 关键实验数据一览

| 配置 | F1 | 样本数 | 说明 |
|---|---|---|---|
| 1_naive（裸LLM）| 0.404 | 161 | 基线下限 |
| 6_full_crag（代码级全套）| 0.419 | 161 | 代码级最优 |
| **kl_rag_only（最优F1）**| **0.443** | **161** | **当前最优** |
| kl_rag_verifier | 0.435 | 161 | 最佳Precision |
| kl_rag_plan | 0.354 | 161 | 最佳Recall=0.944，但FP=122 |
| Cppcheck（传统工具）| 0.359 | 161 | 对比基线（互补关系）|

---

## 各阶段文件对应速查

| 你想了解 | 看这个文件 |
|---|---|
| 数据集如何构建 | `scripts/merge_and_split_dataset.py` |
| KL-RAG 检索逻辑 | `rag/knowledge_retriever.py` |
| 9种 CWE 专用 Prompt | `agents/cwe_prompts.py` |
| 完整 Pipeline 调度 | `pipeline/coordinator.py` |
| Verifier 如何判定漏洞 | `agents/verifier.py` + `agents/sandbox.py` |
| 运行单次实验 | `scripts/evaluate.py` |
| 运行消融实验 | `scripts/run_ablation.py` |
| 实证研究 | `scripts/empirical_study_v3.py` |
| 实验结果数据 | `experiments/` 目录下各 JSON 文件 |
