# 项目核心知识与认知整理

> 这份文档汇总了实验流程之外的所有重要讨论内容,作为新对话的快速入口或答辩前的复习材料。

---

## 第一部分: 项目定位与方向

### 1.1 项目最终定位

**标题方向**:
> 《面向机器人代码的多 Agent 漏洞检测系统:基于 Agentic Corrective RAG 的方法研究》

**关键定位**:
- **范式**: LLM 范式的静态漏洞检测(以静态为主、动态验证为辅)
- **应用场景**: 机器人代码(ROS + 无人机 + 自动驾驶 + 仿真,7 个子领域)
- **技术路线**: Agentic Corrective RAG + Multi-Agent + 沙箱验证

### 1.2 为什么"机器人代码"是最优选择

| 选项 | 工作量 | 论文风险 | 就业价值 | 综合 |
|---|---|---|---|---|
| **机器人代码(当前)** | 0 重做 | 低(蓝海) | 高 | ★★★★★ |
| 通用代码漏洞 | 大 | 极高(红海) | 中 | ★★ |
| 智能合约/DeFi | 6-9 周重做 | 高(红海) | 极高(适合金融) | ★★★ |
| 供应链安全(npm/PyPI) | 1-2 周重做 | 中(蓝海) | 高 | ★★★★ |

**结论**: 机器人代码不是绝对最优,但**对你最划算**——已有数据 + 蓝海 + 专业匹配。

### 1.3 关键认知:你的数据集其实早就不只是 ROS

`COMPONENT_MAP` 把仓库分成 7 类:
- `core_middleware`(ROS 核心)
- `application`(MoveIt, Navigation 等)
- `simulation`(Gazebo, Carla)
- `autonomous_driving`(Apollo, Autoware)
- `drone`(PX4, ArduPilot, MAVROS)
- `perception`(image_pipeline 等)
- `embedded`(micro-ROS)

**所以论文写"机器人代码"比"ROS"更准确,且更宽**。

---

## 第二部分: Agent 与 RAG 的本质理解

### 2.1 Agent 为什么有用(核心理论)

**Agent 不是魔法,F1 提升来自具体的机制,不是来自"叫 Agent"这件事本身**。

3 个真正起作用的机制:

| 机制 | 解决的 LLM 缺陷 | F1 贡献 |
|---|---|---|
| **Verifier(沙箱验证)** | LLM 不会验证,只会猜想 | F1 +2-5%, **Precision +10-20%** |
| **Router(路由专精)** | LLM 注意力分散 | F1 +1-3% |
| **Voting(多次采样)** | LLM 输出有随机性 | F1 +2-5% |

**Agent 的本质**:把 LLM 用对、用足。

- **用对**: 不要一个 prompt 包打天下,分多步调用
- **用足**: 不要只信 LLM 自己,引入外部工具补强它的弱点

### 2.2 RAG 为什么有用(核心理论)

**RAG 给 LLM 引入"训练数据之外的新信息"**——历史漏洞案例。

不同 RAG 增强技术的贡献:

| 技术 | F1 贡献 | 真正机制 |
|---|---|---|
| Hybrid Retrieval | +3-5% | 关键词+语义双通道 |
| HyDE | +1-3% | 缩小 query-doc 鸿沟 |
| Reranker | +2-4% | 强模型精排 |
| CRAG | +2-3% | 质量评估+重试 |

**RAG 总贡献**: 8-15%,比 Agent 还高。

### 2.3 LLM 的根本缺陷

| 缺陷 | 表现 | 解决方式 |
|---|---|---|
| LLM 是生成器不是验证器 | 自信地编造(幻觉) | Verifier 沙箱执行 |
| LLM 注意力有限 | 多任务时关键信息丢失 | Router + 专用 prompt |
| LLM 输出有随机性 | 同一输入不同输出 | Self-Consistency 多次采样 |
| LLM 知识冻结 | 不知道训练后新漏洞 | RAG 检索补充 |

---

## 第三部分: 名词体系(经常混淆)

### 3.1 RAG 名词关系图

```
Agentic RAG (大框架,Agent 化的 RAG)
    │
    ├─ CRAG (质量评估 + 重试机制)
    │
    └─ 检索增强技术:
           ├─ HyDE (查询改写)
           ├─ Hybrid Retrieval (BM25 + Dense)
           └─ Reranker (Cross-Encoder 重排)
```

**一句话区分**:

- **Agentic RAG** = 整个系统(Agent 化的 RAG)
- **CRAG** = 其中的"质量评估 + 重试"机制
- **Hybrid/HyDE/Reranker** = 具体的检索增强技术

### 3.2 Agent 设计模式速查

| 模式 | 含义 | 你项目用了吗 |
|---|---|---|
| **ReAct** | 思考→行动→观察→再思考 | 部分(基础范式) |
| **Plan-and-Execute** | 先规划再执行 | 否 |
| **Reflexion / Reflection** | 完成后自我反思修正 | 推荐加 |
| **Multi-Agent Debate** | 多 Agent 辩论达共识 | 推荐加 |
| **Hierarchical / Supervisor** | 主管协调多个 Worker | ✅ 用了 |
| **Pipeline / Chain** | 固定顺序串行 | ✅ 用了 |
| **Router** | 按特征分发到专家 | ✅ 用了 |
| **Tool Use** | Agent 调用外部工具 | ✅ 用了 |
| **Memory-Augmented** | 持久记忆,跨任务学习 | 可选 |

**你的项目本质 = Hierarchical Multi-Agent Pipeline with Router + Tool Use**。

### 3.3 静态分析 vs 动态分析

| | 静态分析 | 动态分析 |
|---|---|---|
| 怎么做 | 只看代码,不运行 | 实际运行代码 |
| 工具 | Cppcheck/Semgrep/CodeQL | ASan/TSan/Valgrind |
| 找漏洞方式 | 模式匹配 | 触发执行 |
| 速度 | 快 | 慢 |
| 误报率 | 高 | 低 |

**你的项目**:
- Detection Agent 用 LLM + RAG → **静态分析**
- Verifier Agent 用 PoC + 沙箱 → **动态分析**
- 两者结合 = **混合范式**

### 3.4 静态漏洞检测的 6 大范式

| 范式 | 时代 | 代表 |
|---|---|---|
| 1. 基于规则 | 2000s | Cppcheck、Flawfinder |
| 2. 基于符号执行/数据流 | 2010s | CodeQL、Coverity |
| 3. 基于传统机器学习 | 2015 前后 | SVM + 代码特征 |
| 4. 基于图神经网络 | 2018-2020 | Devign、ReVeal |
| 5. 基于预训练代码模型 | 2020-2023 | CodeBERT、GraphCodeBERT |
| **6. 基于 LLM** ← 你属于这个 | **2023 至今** | GPT-4、LLM4Vuln、Vul-RAG |

LLM 范式内部还能细分:
- 6.1 Zero-shot / Naive LLM(最简单)
- 6.2 LLM + RAG
- 6.3 LLM + Fine-tuning
- 6.4 **LLM + RAG + Agent** ← 你属于最前沿

### 3.5 Tools / Skills / MCP / Hooks / Subagents 评估

| 概念 | 工程量 | 论文价值 | 简历价值 | 凑数风险 | 建议 |
|---|---|---|---|---|---|
| Tools / Function Calling | 极小 | 中 | 中 | 低 | 顺手做 |
| **Skills** | 极小 | 低 | 低 | **高** | **不做** |
| **MCP** | 中(9天) | 中 | 高 | 低 | 时间够就做 |
| **Hooks** | 小 | 中 | 中 | 中 | 可做 |
| **Subagents 投票** | 中 | 高 | 极高 | 低 | **推荐做** |
| **Subagents 辩论** | 中 | 极高 | 极高 | 低 | 时间够就做 |

---

## 第四部分: 沙箱与验证机制详解

### 4.1 沙箱(Sandbox)的本质

**沙箱 = 与外界隔离的执行环境,代码在里面跑,搞破坏也炸不到外面**。

为什么必须用:
- PoC 代码可能含恶意行为(命令注入会 `rm -rf /`)
- 在自己电脑上直接跑会真把电脑搞坏
- 沙箱让"危险代码"安全运行

### 4.2 Verifier Agent 怎么判定漏洞?

**核心**: 不是"程序崩了就是漏洞",而是看**崩在什么地方、报什么错**。

关键工具(Sanitizers):

| 工具 | 检测什么 |
|---|---|
| **AddressSanitizer (ASan)** | 缓冲区溢出、UAF、空指针 |
| **ThreadSanitizer (TSan)** | 竞态条件、数据竞争 |
| **UndefinedBehaviorSanitizer (UBSan)** | 整数溢出、未定义行为 |
| **LeakSanitizer (LSan)** | 内存泄漏 |

每种 CWE 对应的判定证据(关键关键词):

| CWE | 关键词 |
|---|---|
| CWE-119 缓冲区溢出 | `stack-buffer-overflow` / `heap-buffer-overflow` |
| CWE-476 空指针 | `SEGV on unknown address 0x000000000000` |
| CWE-401 内存泄漏 | `detected memory leaks` / `Direct leak of N byte` |
| CWE-416 释放后使用 | `heap-use-after-free` |
| CWE-362 竞态 | `data race` |
| CWE-190 整数溢出 | `signed integer overflow` / `unsigned integer overflow` |
| CWE-78 命令注入 | 行为观测(注入命令是否真执行) |
| CWE-134 格式化字符串 | 输出含意外十六进制内容 / 程序崩溃 |

### 4.3 Verifier 输出的 4 种情况

| 场景 | 输出 | 置信度 |
|---|---|---|
| ASan 关键词出现 + 在目标函数调用栈 | ✅ Verified | 0.95 |
| 程序崩了 (SEGV) 但无明确 ASan 信息 | ⚠ Suspected | 0.6 |
| PoC 编译失败(3 次重试后) | ❓ Unverifiable | 0.3 |
| PoC 正常执行无异常 | ❌ Not reproduced | 0.4 |

**关键认知**: Verifier 的真正作用不是把"有"翻成"没有",而是给所有"有漏洞"的判定**分级**(确认/疑似/无法验证)。

---

## 第五部分: 漏洞类型基础(9 种 CWE)

| CWE | 名称 | 一句话记忆 | 占比 |
|---|---|---|---|
| CWE-476 | 空指针解引用 | 用了 NULL 指针,崩 | 27.7%(最多) |
| CWE-401 | 内存泄漏 | 借了不还,越来越多 | 高 |
| CWE-362 | 竞态条件 | 两人同时改一个东西 | 中 |
| CWE-190 | 整数溢出 | 数字太大绕回来了 | 中 |
| CWE-416 | 释放后使用 | 退房了还去开门 | 中 |
| CWE-134 | 格式化字符串 | 让别人写表格格式 | 低 |
| CWE-119 | 缓冲区溢出 | 杯子装不下溢出来 | 中 |
| CWE-78 | 命令注入 | 拼命令被人加私货 | 低 |
| ROS 特定 | 未认证通信 | 谁都能发消息控制机器人 | — |

**为什么只有 9 种?**

实证发现——509 个 ROS 漏洞样本经分类后全部落入这 9 种 CWE,**这是数据说话,不是人为限定**。

---

## 第六部分: ROS 基础知识(最小必备)

### 6.1 一句话定义

**ROS(Robot Operating System)= 机器人软件的通信框架,核心是发布/订阅模式**。

不是真正的操作系统,是中间件。

### 6.2 核心概念(只有 4 个)

| 概念 | 类比 |
|---|---|
| **Node** | 微信群里的一个人 |
| **Topic** | 微信群 |
| **Publish** | 在群里发消息 |
| **Subscribe** | 关注某个群 |

### 6.3 答辩标准回答

**Q: 什么是 ROS?**

> "ROS 是机器人操作系统,采用发布/订阅架构,节点之间通过 Topic 异步通信。代码以回调函数(callback)为主要模式,这也是我们数据集中函数级片段的主要来源。"

**Q: 为什么选 ROS 作为研究对象?**

> "ROS 是全球使用最广泛的机器人开发框架,但其安全机制薄弱,且漏洞可能造成物理世界危害。现有漏洞检测工具主要面向 Web/移动端,缺乏针对机器人代码特征(回调驱动、消息类型依赖、实时性约束)的专用方案。"

**Q: ROS 代码漏洞检测有什么特殊性?**

> "ROS 代码以 callback 形式存在,严重依赖 ROS 消息类型,导致函数级片段无法独立编译运行——这正是我们的 Verification Agent 引入 ROS 类型桩库的动机。"

---

## 第七部分: 必须学习的知识清单

### 第 1 阶段: 基础(必学,2-3 天)

| # | 知识点 | 搜索关键词 |
|---|---|---|
| 1 | RAG 完整流程 | `RAG 检索增强生成 原理` |
| 2 | Embedding + 余弦相似度 | `text embedding 向量相似度` |
| 3 | 向量数据库 Chroma | `chromadb tutorial metadata filter` |
| 4 | LLM Agent 基础(loop + tool use) | `LLM Agent ReAct 入门` |

### 第 2 阶段: 具体技术(必学,2-3 天)

| # | 知识点 | 搜索关键词 |
|---|---|---|
| 5 | BM25 稀疏检索 | `BM25 vs Dense retrieval` |
| 6 | Cross-Encoder 重排 | `cross encoder reranker RAG` |
| 7 | HyDE | `HyDE hypothetical document embeddings` |
| 8 | CRAG / Self-RAG | `corrective RAG self-RAG 论文` |
| 9 | Agentic RAG | `agentic RAG survey 2025` |
| 10 | LangGraph 状态机 | `langgraph tutorial 入门` |

### 第 3 阶段: 高级(选学,1 天)

| # | 知识点 | 搜索关键词 |
|---|---|---|
| 11 | LLM-as-Judge 模式 | `LLM as Judge 评估` |
| 12 | Self-Consistency | `self-consistency LLM 论文` |
| 13 | Multi-Agent 协同模式 | `multi-agent supervisor pattern` |
| 14 | Reflection / Reflexion | `Reflexion NeurIPS 2023` |

### 应付答辩用的最小集

| # | 知识点 | 时间 |
|---|---|---|
| 15 | CWE 9 种背名字 | 1 小时 |
| 16 | ROS 一句话(pub/sub + callback) | 10 分钟 |
| 17 | P/R/F1 公式 | 半天 |
| 18 | Docker 基本命令 | 半天 |

**总学习时间: 约 9 天**,100% 是 AI 工程师面试核心内容。

---

## 第八部分: 论文检索策略

### 8.1 检索平台优先级

| 平台 | 用途 | 网址 |
|---|---|---|
| **arXiv** | 计算机最新论文 95% 在这 | https://arxiv.org |
| **Google Scholar** | 跨平台聚合,看引用数 | https://scholar.google.com |
| **Semantic Scholar** | AI 增强搜索,自动 TLDR | https://www.semanticscholar.org |
| **Connected Papers** | 引用图谱可视化(神器) | https://www.connectedpapers.com |
| **DBLP** | 计算机科学专用文献库 | https://dblp.org |

### 8.2 必读论文清单(按主题)

#### LLM 漏洞检测(必读 5 篇)

```
1. Vul-RAG (Du et al., 2024) ← 你最直接的对手
2. LLM4Vuln (Sun et al., 2024)
3. GPTLens (Hu et al., 2023)
4. "How Effective Are Neural Networks for Fixing Security Vulnerabilities" (2024)
5. VulBench (Gao et al., 2023)
```

#### RAG 必读

```
1. RAG (Lewis et al., NeurIPS 2020) ← 开山之作
2. HyDE (Gao et al., ACL 2023)
3. Self-RAG (Asai et al., ICLR 2024)
4. Corrective RAG / CRAG (Yan et al., 2024)
5. Agentic RAG Survey (Singh et al., 2025)
```

#### Agent 必读

```
1. ReAct (Yao et al., ICLR 2023)
2. Reflexion (Shinn et al., NeurIPS 2023)
3. Self-Consistency (Wang et al., ICLR 2023)
4. Multi-Agent Debate (Du et al., ICML 2024)
5. AutoGen (Wu et al., 2023)
```

#### 漏洞检测经典

```
1. Devign (Zhou et al., NeurIPS 2019)
2. ReVeal (Chakraborty et al., 2021)
3. CodeBERT (Feng et al., 2020)
4. LineVul (Fu & Tantithamthavorn, MSR 2022)
```

#### 机器人安全

```
1. ROBUST: 221 Bugs in ROS (Wang et al., 2020)
2. ROS Security Survey
3. ROSPenTo
```

#### 工具基础

```
1. AddressSanitizer (Serebryany et al., USENIX 2012)
2. ThreadSanitizer (Serebryany & Iskhodzhanov, 2009)
```

---

## 第九部分: 引用规则

### 9.1 引用 = 哪些论文?

**4 类来源**:

| 类别 | 比例 | 例子 |
|---|---|---|
| 1. 你直接用的方法/工具 | 30% | HyDE, CRAG, ASan, LangGraph |
| 2. 你对比的 baseline | 15% | Vul-RAG, Devign, Cppcheck |
| 3. 领域经典/必读 | 30% | RAG by Lewis, ReAct, ROBUST |
| 4. 顺藤摸瓜的相关工作 | 25% | 从上面 1-3 类里追的 |

**总数**: 硕士论文 ~60 篇。

### 9.2 4 个判断问题

写论文时遇到一篇论文,问自己:

1. **我用了它的方法吗?** → 必引
2. **我跟它对比了吗?** → 必引
3. **不引会被认为没看过领域经典吗?** → 必引
4. **它跟我某个具体段落有关联吗?** → 引

**4 个全 No → 不引**(读了就读了)。

### 9.3 引用管理工具

- **Zotero**(免费推荐)— Chrome 插件一键保存
- **Mendeley** — 类似 Zotero
- **EndNote** — 老牌付费

---

## 第十部分: F1 期望与认知校准

### 10.1 SOTA 数据(公开 benchmark)

| 方法 | F1 (Devign) | F1 (BigVul) |
|---|---|---|
| Cppcheck(静态工具) | ~0.30 | ~0.25 |
| CodeBERT 微调 | ~0.62 | ~0.55 |
| GraphCodeBERT | ~0.65 | ~0.58 |
| UnixCoder | ~0.68 | ~0.62 |
| GPT-4 zero-shot | ~0.55 | ~0.50 |
| GPT-4 + simple RAG | ~0.62 | ~0.58 |
| **Vul-RAG (2024 SOTA-ish)** | **~0.72** | **~0.65** |

### 10.2 你的预期 F1

| 配置 | 预期 F1 |
|---|---|
| Baseline (Naive RAG) | 0.65-0.71 |
| +Hybrid Retrieval | 0.68-0.74 |
| +HyDE | 0.69-0.75 |
| +Reranker | 0.71-0.77 |
| +CRAG | 0.73-0.79 |
| +Multi-Agent (含 Router) | 0.75-0.80 |
| +Verification | 0.76-0.81 |

**最终目标**: F1 **0.78-0.82**,跟 Vul-RAG 同水平。

### 10.3 关键认知

1. **不会有戏剧性提升**: 不要期待从 0.71 跳到 0.95
2. **某些技术可能不起作用**: 比如 HyDE 在你的数据集上可能无效,**这正常**
3. **某些组件加上后反而降低**: **这本身也是有价值的发现**
4. **数据集质量决定上限**: 你的数据集是 LLM 标注的,F1 天花板会受限

### 10.4 真正的论文卖点(除了 F1)

如果只比 F1,你可能跟 SOTA 持平。**真正能让你的论文有价值的是**:

1. **可解释性**: 系统给出每个判断的"证据链"
2. **可验证性**: PoC 沙箱执行提供运行时证据,而不是 LLM 嘴炮
3. **领域适配**: 在机器人代码垂直领域,你的方法可能比通用 SOTA 更好
4. **工程完整性**: 完整的 Agent 系统,而不只是一个分类器
5. **消融的科学性**: 每个组件单独验证

---

## 第十一部分: 答辩话术速查

### Q: 为什么用 Multi-Agent,不直接用单个 LLM?

> "我们用 Multi-Agent 不是为了用而用,而是因为它解决了三个具体问题:
> 第一,Router Agent 通过 CWE 类型路由,避免单 LLM 同时考虑 9 种漏洞类型时的注意力稀释,实验显示 +X% F1;
> 第二,Verifier Agent 通过沙箱执行,引入了 LLM 文本输出之外的运行时证据,精确率提升 +X%;
> 第三,Subagent voting 实现 self-consistency,降低 LLM 输出随机性,F1 +X%。
> 我们的消融实验(表 N)清晰展示了每个 Agent 的独立贡献。"

### Q: 你的数据集标签是怎么来的?

> "基于 GitHub 安全提交关键词初筛,再用 LLM 辅助分类,关键样本人工抽查。我们对测试集 220 条进行了 50 条人工二次校验,标注准确率为 X%。"

### Q: 为什么不和 CodeQL/Semgrep 比?

> "我们试过,这两类工具需要完整编译单元,函数级片段无法运行,所以本任务上不适用——这本身也是论文动机之一。"

### Q: 训练测试怎么切分,有没有数据泄漏?

> "我们采用按仓库切分,确保同一仓库的所有样本要么全在训练集要么全在测试集。并且实现了三重检查脚本(ID/代码哈希/仓库级)验证测试集与 RAG 语料无泄漏。"

### Q: 你的 Verifier Agent 怎么保证 PoC 代码执行的安全性?

> "我们使用基于 Docker 的隔离沙箱执行 PoC 代码。沙箱配置了内存/CPU/进程数限制,禁用网络,挂载只读文件系统,并以非 root 用户运行。即使生成的 PoC 含恶意行为,也无法影响主机环境。同时沙箱预装 ASan 等动态分析工具,使得运行时漏洞证据可被自动捕获和解析。"

### Q: 这个工作的创新点在哪?

> "本工作的核心贡献是首次将 Agentic Corrective RAG 范式系统性地应用于机器人代码漏洞检测场景,通过六种检索增强技术的协同(混合召回、元数据过滤、HyDE、重排、CRAG、Agent 迭代),并引入沙箱执行的 Verification Agent 提供运行时证据,在 ROS 函数级漏洞数据集上将 F1 从 0.71 提升到 0.82,且精确率提升 15%。"

---

## 第十二部分: 工业界 vs 学术界的优化思路

### 12.1 优化逻辑相同

```
学术界:
   读论文 → 复现 → 改进 → 发论文

工业界(大厂 AI 团队):
   读论文 (主要 arXiv) → 内部讨论 → 快速验证 (1-2 周) →
   跑业务数据 → 上线 A/B 测试 → 留用或丢弃
```

### 12.2 关键差异

| 维度 | 学术界(写论文) | 工业界(实际上班) |
|---|---|---|
| 优化频率 | 论文周期(几个月) | 持续迭代(几天到几周) |
| 评价标准 | F1 / 论文创新性 | 业务指标(转化率、留存、收入) |
| 验证方式 | 公开 benchmark | 真实用户 A/B 测试 |
| 容忍失败 | 可以"实验未成功" | 失败方法直接丢 |

### 12.3 这个项目对就业的价值

**做这个论文 = 模拟一遍工业界 AI 工程师的日常**。

毕业之后面试 Agent PM,你能讲:

> "我读到 CRAG 论文后,在 1 周内集成到我的系统,做了 5 组消融实验,F1 提升 2.3%。这个过程让我学会了如何评估和落地新技术。"

**这段话比任何课程证书都有说服力**。

---

## 第十三部分: 致命误区警告

### 误区 1: 想做"全新的算法创新"

❌ "我得发明个新算法才能毕业"
✅ 硕士论文 80% 是"组合现有技术 + 实验验证",这是合规的

### 误区 2: 把 Agent 当魔法

❌ "用了 Agent 就一定提升"
✅ Agent 提升来自具体机制(verification, sampling, specialization),不是"叫 Agent"

### 误区 3: 论文最后阶段还在加新功能

❌ "看到 SOTA 论文,赶紧加上"
✅ 最后 2 个月停止追新,稳定优于先进

### 误区 4: 只看 F1 不看其他指标

❌ "F1 跟 SOTA 持平,论文不行"
✅ Precision、Recall、误报率、可解释性都是亮点

### 误区 5: 数据问题不优先解决

❌ "数据有点问题,先做技术再说"
✅ 数据有问题论文全废,先修数据再说

### 误区 6: 加了组件不做消融

❌ "我加了 5 个组件,F1 是 0.82"
✅ 必须出消融表,每加一个组件 F1 提升多少

---

## 第十四部分: 文档清单

桌面上已保存的相关文档:

1. `项目实验流程清单.md` — 完整的 A-F 组任务清单 + 优先级 + 时间方案
2. `硕士论文引用指南.md` — 引用规则 + 工具
3. `静态漏洞分析范式演进.md` — 6 大范式 + LLM 范式细分
4. `项目核心知识与认知整理.md`(本文档) — 实验流程之外的全部知识

项目目录(`robot-llm-vuln-thesis-main/docs/`)下:

1. `FINAL_PLAN.md` — 最终方案概要
2. `CWE_9_TYPES.md` — 9 种 CWE 详解
3. `ROS_BASICS.md` — ROS 基础知识

---

## 第十五部分: 一句话总结(全文档浓缩)

**这个项目的本质**:

> 用 RAG 和 Agent 的各种成熟技术组合起来,解决机器人代码漏洞检测问题,通过充分的对比实验和消融实验证明每个组件的贡献,最终得到一个有说服力的硕士论文 + 一份对 Agent PM 就业有价值的简历项目。

**做这件事的核心原则**:

> **机制驱动,不是名词驱动**。每加一个组件都问"它解决什么具体问题",每个 claim 都要有消融数据支撑。

**最重要的一句话**:

> 时间紧时停止追新,把已有的做扎实比啥都强。

---

## 第十六部分：最新实验认知（2026-06-04 更新）

### 16.1 KL-RAG 的本质（更新理解）

- KL-RAG 存的是**自然语言知识描述**（4个字段），不是代码
- 检索时**两边都要做 LLM 提炼**：建库时提炼一次，查询时也提炼一次
- 召回给 Detection LLM 的是**知识描述文字**，不是原始代码
- raw_code 存在 metadata 里，但当前不传给 Detection LLM

### 16.2 KL-RAG 与其他 RAG 模块的关系

- KL-RAG vs Code-Level RAG：**互斥替代关系**（二选一）
- KL-RAG 内部加 BM25：收益有限（自然语言描述的关键词区分度低于代码）
- **未实验的最佳组合**：KL-RAG(0.7) + Dense代码(0.3) 混合三路召回
- KL-RAG + CRAG：技术上可行，未测试，预期提升 Precision

### 16.3 Few-Shot + CoT Prompt 的教训

qwen-plus（旧 prompt）→ F1=0.443，Recall=0.750
qwen-max（Few-Shot+CoT）→ F1=0.388，Recall=0.361

**核心原因**：CoT 的第 3 步 "DEFEND: 有没有防护措施" + "DO NOT flag" 例子，让 LLM 主动找理由不报漏洞，真实漏洞置信度被压到 < 0.3。

**教训**：Few-Shot 中的 SAFE 例子 + 保守指令会显著降低 Recall，在类别不平衡（80% clean）数据集上尤为严重。

### 16.4 Plan-and-Solve 的局限

- 5次 LLM 调用，Recall=0.944 但 FP=122，F1=0.354
- **根本原因**：函数级片段缺乏完整上下文，步骤 3 问"有没有防护"，LLM 看不到调用方的检查，误判"没防护" → 大量误报
- 不是 ReAct 做得不好，是**数据本身的局限**：函数级 ≠ 项目级

### 16.5 Agent 单轮 vs 多轮

- 大多数 Agent（Router/Detection/CRAG）是**单轮**，严格说是"智能节点"
- Verifier 接近 ReAct（LLM→工具→观察→判断）
- Plan-and-Solve 是**真正多轮**（5次串行 LLM）
- Voting 是**并行多次**（3个 LLM 同时调用）
- **ReAct 必须多轮**，单轮只能算"带工具的节点"

### 16.6 MCP 的定位

- 本质：**Tool 和 Agent 解耦**
- **不提升 F1，不加速运行**
- 价值：代码可维护性/扩展性，换工具不改 Agent 代码
- 适合时机：论文写完后，作为工程优化方向

### 16.7 待验证的潜在提升方向（优先级排序）

1. **恢复旧 prompt + 保留 qwen-max**：快速验证 LLM 升级本身是否有正面效果
2. **KL-RAG + CRAG**：检索质量兜底，预期降低误报
3. **KL-RAG + Code-Level Hybrid（三路混合）**：互补两种检索范式
4. **Cppcheck as Tool**：`代码 + Cppcheck结果 + KL案例 → Detection LLM`，提升 CWE-476/416 Recall
5. **VulAgent 风格多轮验证**：Hypothesis + Verification，改善 PoC 精准度
